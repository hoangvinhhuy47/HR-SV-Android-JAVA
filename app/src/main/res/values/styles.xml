<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="BottomNavigationTheme" parent="Theme.AppCompat.Light">
        <item name="colorPrimary">@color/mautrang</item>
        <item name="android:textColorSecondary">@color/mauden</item>
    </style>
    <style name="spinner_style">
        <item name="android:textColor">@color/mauden</item>
        <item name="android:textSize">@dimen/TextSize14</item>
        <item name="android:paddingLeft">0dp</item>
        <item name="android:showDividers">none</item>
        <item name="android:dividerHeight">0dp</item>
        <item name="android:popupBackground">@drawable/textboxaccount2</item>
        <item name="android:dropDownHeight">70dp</item>
        <item name="android:scrollbarAlwaysDrawVerticalTrack">true</item>
        <item name="android:dropDownSelector">@android:color/white</item>
    </style>
    <style name="TimePicker_style">
        <item name="android:textSize">@dimen/TextSize15</item>
        <item name="android:scaleY">1</item>
        <item name="android:scaleX">1</item>
    </style>
</resources>